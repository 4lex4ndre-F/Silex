{% extends "layout.html.twig" %}

{% block title 'Admin rubrique' %}
    
{% block content %}
    <h1>Gestion Rubriques</h1>
    
    <table class='table'>
        <tr>
            <th>id</th>
            <th>Nom</th>
        </tr> 
        {% for category in categories %}
            <tr>
                <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
            </tr>
        {% endfor %}
    </table>
    
    
{% endblock %}
